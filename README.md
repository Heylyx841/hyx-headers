# hyx-headers

一个基于 C++ 的 header-only 工具库集合。

## 组件列表

### 1. `hyx::autoseq<T> (C++20)`
一个动态数学数列容器。

- **延迟计算**: 仅在访问时按需生成数列项。
- **自动缓存**: 每一项仅计算一次，后续访问为 $O(1)$。
- **数学直觉 API**: 在公式中直接使用 `F.last()` 或 `F[i]`。
